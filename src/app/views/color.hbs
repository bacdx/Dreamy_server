
<div class="col-6 justyfy-content-end">
  <a href="/product/addColor/{{masp.id}}" type="button" class="btn btn-primary align-self-center">+ add color</a>
</div>
  <form class="d-flex" method="POST" action="#">
    <input type="sreach" class="from-control me-2"  placeholder="Search" aria-label="Search" name="search">
    <button class="btn" type="submit">Search</button>
  </form>
<table class="table table-bordered mt-4 ">
  <thead class="thead-dark">
    <tr>
      <th scope="col">id</th>
      <th scope="col">Tên</th>
      <th scope="col">Mã màu</th>
       <th scope="col">id sp</th>
        <th scope="col" class="text-end">trạng thái</th>
    </tr>
  </thead>
  <tbody>
      {{#each data}}
    <tr>
       
      <th scope="row">{{this.id}}</th>
      <td>{{this.title}}</td>
      <td>{{this.mamau}}</td>
     <td>{{this.masp}}</td>
     <td class="text-end">
       
        <a href="/product/editColorFirm/{{this.id}}" type="button" class="btn btn-light btn-small"><i class="bi bi-pencil"></i> Edit</a>
        <a href="/product/deleteColor/{{this.id}}" type="button" class="btn btn-light btn-small"><i class="bi bi-pencil"></i>Delete</a>
      
      
      </td>
     
    </tr>
    {{/each}}

  </tbody>
</table>